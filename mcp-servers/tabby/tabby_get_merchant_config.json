{
  "name": "tabby_get_merchant_config",
  "description": "Retrieve Tabby merchant configuration, checkout limits, supported currencies, and test mode status.",
  "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "merchant_code": {
        "type": "string",
        "description": "Tabby merchant identifier code"
      }
    },
    "additionalProperties": false
  }
}
