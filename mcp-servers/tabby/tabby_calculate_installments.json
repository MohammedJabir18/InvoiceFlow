{
  "name": "tabby_calculate_installments",
  "description": "Calculate 4-payment interest-free installment schedule and eligibility for a given amount and currency.",
  "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "amount": {
        "type": "number",
        "description": "Invoice or order total amount"
      },
      "currency": {
        "type": "string",
        "description": "ISO currency (AED, SAR, KWD, BHD, USD)",
        "default": "AED"
      }
    },
    "required": ["amount"],
    "additionalProperties": false
  }
}
